{
  "name": "ses-compartments-mf-event-bus-poc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @poc/host dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test && pnpm exec vitest run",
    "demo:node": "pnpm --filter @poc/host demo:node",
    "dev:remotes": "pnpm --filter @poc/catalog dev & pnpm --filter @poc/cart dev"
  },
  "devDependencies": {
    "vitest": "^3.2.6"
  },
  "dependencies": {
    "ses": "^1.15.0",
    "zod": "^3.25.76"
  }
}
