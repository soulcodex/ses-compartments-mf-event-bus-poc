{
  "name": "@poc/catalog",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "rsbuild dev --port 3001",
    "build": "rsbuild build",
    "serve": "node ../../scripts/origin-server.mjs --dist dist --port 4001 --role catalog --origin http://localhost:4001"
  },
  "dependencies": {
    "@poc/shared": "workspace:*",
    "ses": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@module-federation/rsbuild-plugin": "^2.5.0",
    "@rsbuild/core": "^2.0.11",
    "@types/node": "^22.0.0",
    "serve": "^13.0.4",
    "typescript": "^5.7.0"
  }
}
